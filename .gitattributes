# Normalize line endings: text files are LF in the repo, auto-converted on checkout.
* text=auto eol=lf

# Binary assets (never normalize / diff).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webm binary
*.mp4 binary
*.zip binary
*.pdf binary
